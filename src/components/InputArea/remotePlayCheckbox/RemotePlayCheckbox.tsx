import { Checkbox } from "@mui/material";
import { FC } from "react";

/**
 * リモート対戦可否チェックボックス
 *
 * @returns リモート対戦可否チェックボックス
 */
export const RemotePlayCheckbox: FC = () => {
  return <Checkbox />;
};
